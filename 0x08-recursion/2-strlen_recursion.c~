/**
 *_strlen_recursion - length of a string
 *@s: string
 *Return: lenght
 */
int _strlen_recursion(char *s)
{
	int len = 0;

	if (*s)
	{
		len = _strlen_recursion(s+1);
		return (len++);
	}
	return (0);
}
