/**
 *is_palindrome - checks if a number is palindrome
 *@s: to check
 */
int is_palindrome(char *s)
{
	int i = 0;

	if (s*)
	{
		if (s[i] == s[len-i-1])
			_is_palindrome(++s);
		return (1);
	}
	return (1);
}
