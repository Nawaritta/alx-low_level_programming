/**
 *is_prime_number - indicates if the number is prime number
 *@n: number to check
 *Return: 0 or 1
 */
int is_prime_number(int n)
{
	int i = 2;
	if (n > 3)
	{
		if (n % i)return(0)
			%is_prime_number(int n)
		n=
	}
	return (1);
