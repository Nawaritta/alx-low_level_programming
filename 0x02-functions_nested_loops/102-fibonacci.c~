#include <stdio.h>
/**
 *main - entry point
 *
 *return: always zero
 */
int main(void)
{
	int a = 0;
	int b = 1;
	int c = a + b;
	int i = 2;

	while (i < 50)
	{
		c = a + b;
		a = b;
		b = c;
		i++;
	}
	printf("%d\n", c);
	return (0);
}
